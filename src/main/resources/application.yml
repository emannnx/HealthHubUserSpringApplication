server:
  port: 8099

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/HealthHubUsers?createDatabaseIfNotExist=true
    username: root
    password: Stanleyobi123!
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
